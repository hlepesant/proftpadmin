{{ form_start(delete_form) }}
{{ form_widget(delete_form) }}
<input type="image" src="{{ asset('/media/famfam/cross.png') }}" alt="Submit button" name="type" value="submit" />
{{ form_end(delete_form) }}
