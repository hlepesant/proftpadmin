{% extends 'base.html.twig' %}

{% block title %}Edit FtpUser{% endblock %}

{% block body %}
    <h1>Change Password for {{ ftp_user.username }}</h1>

    {{ include('ftp_user/_form.html.twig', {'button_label': 'Update'}) }}

    {# <a href="{{ path('ftp_user_index', {'id_group': ftp_group.id}) }}"><button type="button" class="btn btn-secondary">Cancel</button></a> #}

{% endblock %}
